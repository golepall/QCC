# QCC 真实开机自动续跑现场验收记录模板

生成日期：2026-06-12

## 1. 基本信息

- 项目编号：
- 模板编号：
- 任务类型：`s3_cycle / s4_cycle / reboot_cycle`
- Agent exe 路径：
- 测试包路径：
- 验收机器名称：
- 操作系统版本：
- 测试用户：
- 验收日期：
- 验收人员：
- 陪同人员：

## 2. 验收依据

- 验收步骤文档：`d:\QCC\docs\真实开机自动续跑现场验收步骤.md`
- 当前阶段文档：`d:\QCC\docs\自动化测试阶段验收与残余缺口.md`

## 3. 环境前置检查

| 检查项 | 期望结果 | 实际结果 | 是否通过 |
| --- | --- | --- | --- |
| Windows 环境 | 验收机器为 Windows |  |  |
| 当前用户可登录 | 可使用测试用户正常登录桌面 |  |  |
| Agent 可启动 | `QCC_Test_Agent.exe` 可正常打开 GUI |  |  |
| `%APPDATA%` 可写 | 可访问并写入 Startup 目录 |  |  |
| 测试包可用 | 测试包目录存在且未损坏 |  |  |

## 4. 步骤执行记录

| 步骤 | 操作内容 | 期望结果 | 实际结果 | 证据路径/截图 | 是否通过 |
| --- | --- | --- | --- | --- | --- |
| 4.1 | 创建 `S3/S4/重启循环` 计划 | 已生成 `agent_runtime\long_task_state.json`，并出现对应 `task_key` |  |  |  |
| 4.2 | 点击“标记等待恢复” | 已生成 `resume_note.txt`、`resume_launcher.cmd`、`resume_launcher.ps1` |  |  |  |
| 4.2 | 检查状态文件 | `resume_state=waiting_resume`，`startup_registered=true` |  |  |  |
| 4.3 | 检查 Startup 注册 | Startup 目录中存在 `QCC_AutoResume_<project_code>_<task_key>.cmd` |  |  |  |
| 4.4 | 检查恢复启动脚本 | 启动脚本包含 `--resume-long-task <task_key>` |  |  |  |
| 4.5 | 执行一次恢复流程并登录 | 登录后 Agent 自动拉起 |  |  |  |
| 4.5 | 观察恢复动作 | Agent 自动定位到对应长任务，不需要手工点击“恢复继续” |  |  |  |
| 4.6 | 检查恢复后状态 | `resume_state=resumed`，`status=running`，`resume_count` 增加 |  |  |  |
| 4.6 | 检查 Startup 撤销 | `startup_registered=false`，原 Startup 启动脚本已移除 |  |  |  |
| 4.7 | 继续记录直到计划完成 | 任务状态变为 `completed`，`resume_state` 被清空 |  |  |  |
| 4.7 | 检查完成态清理 | Startup 目录中不再存在对应启动脚本 |  |  |  |
| 4.8 | 手工点击“清理续跑文件” | 续跑说明、启动脚本、Startup 启动脚本均被删除 |  |  |  |

## 5. 关键文件记录

| 文件/路径 | 期望状态 | 实际状态 | 备注 |
| --- | --- | --- | --- |
| `agent_runtime\long_task_state.json` | 恢复前后状态符合预期 |  |  |
| `agent_runtime\<task_key>_resume_note.txt` | 标记等待恢复后存在 |  |  |
| `agent_runtime\<task_key>_resume_launcher.cmd` | 内容为真实启动命令 |  |  |
| `agent_runtime\<task_key>_resume_launcher.ps1` | 内容为真实启动命令 |  |  |
| `%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\QCC_AutoResume_<project_code>_<task_key>.cmd` | 等待恢复时存在，恢复后或清理后移除 |  |  |

## 6. 核心状态摘录

### 6.1 恢复前

```json
{
  "key": "",
  "status": "",
  "resume_state": "",
  "resume_count": 0,
  "startup_registered": false,
  "startup_launcher_cmd": ""
}
```

### 6.2 恢复后

```json
{
  "key": "",
  "status": "",
  "resume_state": "",
  "resume_count": 0,
  "last_resume_at": "",
  "startup_registered": false
}
```

### 6.3 完成态或清理后

```json
{
  "key": "",
  "status": "",
  "resume_state": "",
  "next_action": "",
  "startup_registered": false,
  "startup_launcher_cmd": ""
}
```

## 7. 异常记录

| 序号 | 异常现象 | 发生步骤 | 初步原因 | 是否已复现 | 处理结果 |
| --- | --- | --- | --- | --- | --- |
| 1 |  |  |  |  |  |
| 2 |  |  |  |  |  |
| 3 |  |  |  |  |  |

## 8. 结论

- 是否通过本轮现场验收：
- 未通过时的阻塞原因：
- 是否建议进入下一轮优化：
- 下一轮建议项：

## 9. 签字

- 验收人员：
- 研发确认：
- 项目负责人：
